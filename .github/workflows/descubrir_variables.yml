name: Descubrir variables de viento SMN

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  descubrir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependencias
        run: pip install requests netCDF4 numpy

      - name: Descubrir variables del archivo NetCDF
        run: python descubrir_variables_viento.py
